Now can we conclude that if we run 'make -f Makefile_windows dev', it can completely replace the below commands:
- 'pip install poetry'
- 'poetry install --no-root'
- 'poetry show --only main'
- 'npm install'
- 'npm list'
- 'choco install make'
